# Copy to .env and edit before: docker compose --env-file .env up --build

POSTGRES_USER=kongmanager
POSTGRES_PASSWORD=change-me-db
POSTGRES_DB=kong_manager

JWT_SECRET=change-me-long-random-string
BOOTSTRAP_ADMIN_PASSWORD=change-me-admin

KONG_MANAGER_PORT=8080

# Kong Admin URL reachable from the BFF container (Docker Desktop: host.docker.internal)
KONG_ADMIN_URL=http://host.docker.internal:8001

PUBLIC_BASE_URL=
COOKIE_SECURE=false

# SPA static prefix — must match Vite `base` (default /__km_base__). Empty only if build used DISABLE_BASE_PATH=true.
# ADMIN_GUI_PATH=/__km_base__

# Verify Kong Admin HTTPS with your CA (PEM path inside container). Prefer over KONG_UPSTREAM_TLS_SKIP_VERIFY.
# KONG_UPSTREAM_TLS_CA_FILE=/etc/kong-manager/tls/kong-admin-ca.pem
